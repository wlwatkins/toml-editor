// The desktop build is a static SPA loaded from a custom protocol: there is no
// server to render on, and every route is reached by client-side navigation.
export const prerender = false;
export const ssr = false;
